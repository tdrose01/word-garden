export const levels = [
  {
    id: 1,
    title: 'Meadow',
    pack: 'Seedling Path',
    letters: 'PLANT',
    targets: ['PLANT', 'PLAN', 'PANT', 'ANT', 'TAP'],
    bonus: ['NAP', 'PAN', 'PAT', 'TAN', 'LAP', 'PAL', 'APT']
  },
  {
    id: 2,
    title: 'Brook',
    pack: 'Seedling Path',
    letters: 'STONE',
    targets: ['STONE', 'TONE', 'ONES', 'NOTES', 'TOE'],
    bonus: ['SET', 'TEN', 'NET', 'SON', 'ONE', 'TON', 'NOTE']
  },
  {
    id: 3,
    title: 'Orchard',
    pack: 'Seedling Path',
    letters: 'BRANCH',
    targets: ['BRANCH', 'RANCH', 'CHAR', 'BARN', 'CAR'],
    bonus: ['CAN', 'BAR', 'ARC', 'CAB', 'RAN', 'BAN', 'BRA']
  },
  {
    id: 4,
    title: 'Fern',
    pack: 'Seedling Path',
    letters: 'GARDEN',
    targets: ['GARDEN', 'GRADE', 'RANGE', 'NEAR', 'RAG'],
    bonus: ['AGE', 'EAR', 'RED', 'DEN', 'DARN', 'DANGER', 'READ']
  },
  {
    id: 5,
    title: 'Canopy',
    pack: 'Seedling Path',
    letters: 'FOREST',
    targets: ['FOREST', 'FROST', 'STORE', 'ROSE', 'TOE'],
    bonus: ['FOR', 'SET', 'REST', 'SOFT', 'SORT', 'ORES', 'TORE']
  },
  {
    id: 6,
    title: 'Moss Gate',
    pack: 'Moss Trail',
    letters: 'MALLOW',
    targets: ['MALLOW', 'ALLOW', 'MALL', 'WALL', 'LOW'],
    bonus: ['LAW', 'OWL', 'MOW', 'AWL']
  },
  {
    id: 7,
    title: 'Cedar',
    pack: 'Moss Trail',
    letters: 'CEDAR',
    targets: ['CEDAR', 'DARE', 'READ', 'CARE', 'RED'],
    bonus: ['ACE', 'ARC', 'ARE', 'CAR', 'EAR', 'ERA', 'CAD']
  },
  {
    id: 8,
    title: 'Wander',
    pack: 'Moss Trail',
    letters: 'WANDER',
    targets: ['WANDER', 'WARD', 'DARE', 'NEAR', 'RAN'],
    bonus: ['AND', 'ARE', 'AWE', 'DEN', 'NEW', 'RED', 'WREN']
  },
  {
    id: 9,
    title: 'Pollen',
    pack: 'Moss Trail',
    letters: 'POLLEN',
    targets: ['POLLEN', 'POLE', 'POLL', 'LONE', 'PEN'],
    bonus: ['ONE', 'ELL', 'LOPE', 'NOEL', 'OPEN', 'PEON', 'OLE']
  },
  {
    id: 10,
    title: 'Riverbed',
    pack: 'Moss Trail',
    letters: 'SPROUT',
    targets: ['SPROUT', 'SPOUT', 'PORT', 'ROUT', 'TOP'],
    bonus: ['POT', 'OUT', 'OUR', 'RUT', 'SORT', 'STOP', 'POUR']
  },
  {
    id: 11,
    title: 'Laurel',
    pack: 'Grove Rise',
    letters: 'LAUREL',
    targets: ['LAUREL', 'LURE', 'REAL', 'RULE', 'EARL'],
    bonus: ['ALE', 'ALL', 'ARE', 'EAR', 'ERA', 'RUE', 'UREA']
  },
  {
    id: 12,
    title: 'Thicket',
    pack: 'Grove Rise',
    letters: 'THICKET',
    targets: ['THICKET', 'THICK', 'TICK', 'KITE', 'HIT'],
    bonus: ['THE', 'TIE', 'KIT', 'ICE', 'TIC', 'CHIT', 'ETCH']
  },
  {
    id: 13,
    title: 'Sunrise',
    pack: 'Canopy Climb',
    letters: 'SUNRISE',
    targets: ['SUNRISE', 'RINSE', 'RISEN', 'SURE', 'SUN'],
    bonus: ['RISE', 'RUNE', 'RUN', 'SIN', 'SIR', 'USER', 'USE']
  },
  {
    id: 14,
    title: 'Bloom',
    pack: 'Canopy Climb',
    letters: 'BLOOM',
    targets: ['BLOOM', 'BOOM', 'LOOM', 'MOB', 'BOO'],
    bonus: ['LOB', 'MOO']
  },
  {
    id: 15,
    title: 'Willow',
    pack: 'Canopy Climb',
    letters: 'WILLOW',
    targets: ['WILLOW', 'WILL', 'LOW', 'OWL', 'ILL'],
    bonus: ['OIL', 'WOW']
  },
  {
    id: 16,
    title: 'Pebble',
    pack: 'Canopy Climb',
    letters: 'PEBBLE',
    targets: ['PEBBLE', 'PEEL', 'BEEP', 'BEE', 'PEE'],
    bonus: ['EEL', 'LEE', 'BLEEP', 'EBB']
  },
  {
    id: 17,
    title: 'Rapids',
    pack: 'Canopy Climb',
    letters: 'RAPIDS',
    targets: ['RAPIDS', 'PAIRS', 'RAIDS', 'PAID', 'SAID'],
    bonus: ['AIR', 'AID', 'DIP', 'RAP', 'RID', 'SIP', 'SPA']
  },
  {
    id: 18,
    title: 'Hollow',
    pack: 'Canopy Climb',
    letters: 'HOLLOW',
    targets: ['HOLLOW', 'HOWL', 'WOOL', 'LOW', 'OWL'],
    bonus: ['WHO', 'HOO', 'HOW', 'LOO', 'WOO']
  },
  {
    id: 19,
    title: 'Thorns',
    pack: 'Summit Grove',
    letters: 'THORNS',
    targets: ['THORNS', 'HORNS', 'NORTH', 'SHORT', 'HOST'],
    bonus: ['SON', 'SORT', 'TON', 'TONS', 'HOT', 'NOR', 'ROT']
  },
  {
    id: 20,
    title: 'Petunia',
    pack: 'Summit Grove',
    letters: 'PETUNIA',
    targets: ['PETUNIA', 'PAINT', 'UNITE', 'TUNA', 'ANTI'],
    bonus: ['ANT', 'TEA', 'TIE', 'NAP', 'PEN', 'UNIT', 'TUNE']
  },
  {
    id: 21,
    title: 'Marigold',
    pack: 'Summit Grove',
    letters: 'MARIGOLD',
    targets: ['MARIGOLD', 'RADIO', 'ROAM', 'GOAL', 'MAIL'],
    bonus: ['GILD', 'GIRL', 'RAIL', 'LOAD', 'ROAD', 'LARD', 'MOLD']
  },
  {
    id: 22,
    title: 'Canyon',
    pack: 'Summit Grove',
    letters: 'CANYON',
    targets: ['CANYON', 'CANON', 'CANNY', 'ANY', 'CON'],
    bonus: ['CAN', 'COY', 'NAY']
  },
  {
    id: 23,
    title: 'Cypress',
    pack: 'Summit Grove',
    letters: 'CYPRESS',
    targets: ['CYPRESS', 'PRESS', 'CRESS', 'CRY', 'YES'],
    bonus: ['RYE', 'SPY', 'PREY', 'PER']
  },
  {
    id: 24,
    title: 'Harvest',
    pack: 'Summit Grove',
    letters: 'HARVEST',
    targets: ['HARVEST', 'HEART', 'STARE', 'EARTH', 'HARE'],
    bonus: ['STAR', 'HATE', 'HAVE', 'HEAR', 'TEAR', 'VASE', 'REST']
  },
  {
    id: 25,
    title: 'Cloudbank',
    pack: 'Ridge Bloom',
    letters: 'CLOUDS',
    targets: ['CLOUDS', 'CLOUD', 'COLD', 'LOUD', 'SOLD'],
    bonus: ['COD', 'OLD', 'SOD', 'SOUL', 'DUO']
  },
  {
    id: 26,
    title: 'Moonlit',
    pack: 'Ridge Bloom',
    letters: 'MOONLIT',
    targets: ['MOONLIT', 'MOON', 'LOOM', 'LION', 'MINT'],
    bonus: ['INTO', 'TOON', 'MOOT', 'OMIT', 'OIL', 'TIN', 'LIT']
  },
  {
    id: 27,
    title: 'Glacier',
    pack: 'Ridge Bloom',
    letters: 'GLACIER',
    targets: ['GLACIER', 'RAGE', 'REAL', 'RAIL', 'GIRL'],
    bonus: ['ACE', 'AGE', 'AIR', 'ALE', 'ARC', 'EAR', 'ERA', 'ICE', 'LIE']
  },
  {
    id: 28,
    title: 'Orchid',
    pack: 'Ridge Bloom',
    letters: 'ORCHID',
    targets: ['ORCHID', 'CHORD', 'RICH', 'CORD', 'COD'],
    bonus: ['HID', 'RID', 'DOC']
  },
  {
    id: 29,
    title: 'Maples',
    pack: 'Ridge Bloom',
    letters: 'MAPLES',
    targets: ['MAPLES', 'MAPLE', 'LEAPS', 'MALES', 'SEAL'],
    bonus: ['PALM', 'MEAL', 'PEAL', 'SALE', 'SEAM', 'LAP', 'PAL', 'ALE', 'SEA']
  },
  {
    id: 30,
    title: 'Rooted',
    pack: 'Ridge Bloom',
    letters: 'ROOTED',
    targets: ['ROOTED', 'ROOT', 'DOOR', 'TORE', 'RODE'],
    bonus: ['TOE', 'RED', 'ORE', 'ROT', 'DOT', 'DOE', 'TOO', 'ODE']
  },
  {
    id: 31,
    title: 'Lavender',
    pack: 'Wildflower Peak',
    letters: 'LAVENDER',
    targets: ['LAVENDER', 'LEARNED', 'LEAN', 'DEAL', 'DEER'],
    bonus: ['LANE', 'EARN', 'DARE', 'NEAR', 'REAL', 'READ', 'VEAL', 'VANE', 'DARN']
  },
  {
    id: 32,
    title: 'Jasmine',
    pack: 'Wildflower Peak',
    letters: 'JASMINE',
    targets: ['JASMINE', 'MEANS', 'AMEN', 'MINE', 'JAMS'],
    bonus: ['MAIN', 'NAME', 'SAME', 'SANE', 'SEAM', 'MAN', 'MEN', 'SIN']
  },
  {
    id: 33,
    title: 'Dahlia',
    pack: 'Wildflower Peak',
    letters: 'DAHLIA',
    targets: ['DAHLIA', 'HAIL', 'LAID', 'DIAL', 'AID'],
    bonus: ['HAD', 'LAD', 'LID', 'AIL']
  },
  {
    id: 34,
    title: 'Terrace',
    pack: 'Wildflower Peak',
    letters: 'TERRACE',
    targets: ['TERRACE', 'TRACE', 'CATER', 'REACT', 'TREE'],
    bonus: ['TEAR', 'RATE', 'CART', 'CARE', 'RACE', 'TEA']
  },
  {
    id: 35,
    title: 'Wildwood',
    pack: 'Wildflower Peak',
    letters: 'WILDWOOD',
    targets: ['WILDWOOD', 'WILD', 'WOOD', 'WOOL', 'OWL'],
    bonus: ['LOW', 'OLD', 'OIL', 'LID', 'WOO']
  },
  {
    id: 36,
    title: 'Evergreen',
    pack: 'Wildflower Peak',
    letters: 'EVERGREEN',
    targets: ['EVERGREEN', 'GREEN', 'NEVER', 'EVEN', 'VEER'],
    bonus: ['EVER', 'EVE', 'GENRE', 'GENE', 'VENEER']
  }
];

export const dailyLevels = [
  {
    id: 'daily-fern',
    title: 'Daily Fern',
    letters: 'MORNING',
    targets: ['MORNING', 'MINOR', 'RING', 'GRIN', 'IRON'],
    bonus: ['GIN', 'NOR', 'RIM', 'ROAM', 'GROIN', 'NORM', 'MORN']
  },
  {
    id: 'daily-moss',
    title: 'Daily Moss',
    letters: 'PETALS',
    targets: ['PETALS', 'PLEAT', 'LEAP', 'PAST', 'TEA'],
    bonus: ['SEA', 'EAT', 'SAT', 'PAL', 'SALE', 'SEAL', 'PALE']
  },
  {
    id: 'daily-sage',
    title: 'Daily Sage',
    letters: 'CLOVER',
    targets: ['CLOVER', 'COVER', 'LOVE', 'CORE', 'ROE'],
    bonus: ['ORE', 'COLE', 'ROLE', 'COVE', 'LORE', 'OVER', 'VOLE']
  },
  {
    id: 'daily-bloom',
    title: 'Daily Bloom',
    letters: 'VIOLET',
    targets: ['VIOLET', 'VOTE', 'TOIL', 'LIVE', 'TIE'],
    bonus: ['OIL', 'LIE', 'LET', 'VIE', 'VILE', 'TOE', 'LOT']
  },
  {
    id: 'daily-grove',
    title: 'Daily Grove',
    letters: 'SUMMER',
    targets: ['SUMMER', 'MUSE', 'SURE', 'RUM', 'USE'],
    bonus: ['SUM', 'EMU', 'RUE', 'RUSE', 'USER', 'REM', 'MUM']
  }
];

export function getDailyLevel(date = new Date()) {
  const key = getDateKey(date);
  const seed = Array.from(key).reduce((total, char) => total + char.charCodeAt(0), 0);

  return {
    ...dailyLevels[seed % dailyLevels.length],
    id: key,
    title: `Daily ${key.slice(5)}`
  };
}

export function getDateKey(date = new Date()) {
  return date.toISOString().slice(0, 10);
}

export function buildGrid(targets) {
  const placements = [];
  const occupied = new Map();

  function addPlacement(placement) {
    placements.push(placement);
    Array.from(placement.word).forEach((letter, index) => {
      const x = placement.x + (placement.direction === 'across' ? index : 0);
      const y = placement.y + (placement.direction === 'down' ? index : 0);
      occupied.set(cellKey(x, y), letter);
    });
  }

  targets.forEach((word, index) => {
    if (index === 0) {
      addPlacement({ word, x: 0, y: 2, direction: 'across' });
      return;
    }

    const placement = placements.length < 3
      ? findCleanDownPlacement(targets[0], word, occupied)
      : findCompactCrossPlacement(word, placements, occupied);
    if (placement) {
      addPlacement(placement);
      return;
    }

    addPlacement({
      word,
      x: 0,
      y: findNextRowBelow(placements),
      direction: 'across'
    });
  });

  return normalizePlacements(placements);
}

function findCleanDownPlacement(baseWord, word, occupied) {
  for (let wordIndex = 0; wordIndex < word.length; wordIndex += 1) {
    for (let baseIndex = 0; baseIndex < baseWord.length; baseIndex += 1) {
      if (baseWord[baseIndex] !== word[wordIndex]) continue;
      const placement = {
        word,
        x: baseIndex,
        y: 2 - wordIndex,
        direction: 'down'
      };
      if (canPlace(placement, occupied)) return placement;
    }
  }
  return null;
}

function findCompactCrossPlacement(word, placements, occupied) {
  const candidates = [];

  placements.forEach((existing) => {
    Array.from(existing.word).forEach((existingLetter, existingIndex) => {
      const existingX = existing.x + (existing.direction === 'across' ? existingIndex : 0);
      const existingY = existing.y + (existing.direction === 'down' ? existingIndex : 0);

      Array.from(word).forEach((letter, wordIndex) => {
        if (letter !== existingLetter) return;
        const direction = existing.direction === 'across' ? 'down' : 'across';
        const placement = {
          word,
          x: existingX - (direction === 'across' ? wordIndex : 0),
          y: existingY - (direction === 'down' ? wordIndex : 0),
          direction
        };
        if (canPlace(placement, occupied)) candidates.push(placement);
      });
    });
  });

  return candidates.sort((left, right) => comparePlacementFit(left, right, placements))[0] ?? null;
}

function comparePlacementFit(left, right, placements) {
  const leftBounds = getPlacementBounds([...placements, left]);
  const rightBounds = getPlacementBounds([...placements, right]);
  const leftArea = leftBounds.width * leftBounds.height;
  const rightArea = rightBounds.width * rightBounds.height;

  return leftArea - rightArea
    || Math.max(leftBounds.width, leftBounds.height) - Math.max(rightBounds.width, rightBounds.height)
    || leftBounds.width - rightBounds.width
    || left.y - right.y
    || left.x - right.x;
}

function getPlacementBounds(placements) {
  const cells = placements.flatMap((placement) => Array.from(placement.word).map((_, index) => ({
    x: placement.x + (placement.direction === 'across' ? index : 0),
    y: placement.y + (placement.direction === 'down' ? index : 0)
  })));
  const xs = cells.map((cell) => cell.x);
  const ys = cells.map((cell) => cell.y);
  return {
    width: Math.max(...xs) - Math.min(...xs) + 1,
    height: Math.max(...ys) - Math.min(...ys) + 1
  };
}

function canPlace(placement, occupied) {
  let hasNewCell = false;
  const hasOnlyCompatibleCells = Array.from(placement.word).every((letter, index) => {
    const x = placement.x + (placement.direction === 'across' ? index : 0);
    const y = placement.y + (placement.direction === 'down' ? index : 0);
    const current = occupied.get(cellKey(x, y));
    hasNewCell = hasNewCell || current === undefined;
    return current === undefined || current === letter;
  });

  return hasOnlyCompatibleCells && hasNewCell;
}

function findNextRowBelow(placements) {
  const maxY = Math.max(
    ...placements.flatMap((placement) =>
      Array.from(placement.word).map((_, index) => placement.y + (placement.direction === 'down' ? index : 0))
    )
  );
  return maxY + 2;
}

function cellKey(x, y) {
  return `${x}:${y}`;
}

function normalizePlacements(placements) {
  const cells = placements.flatMap((placement) =>
    Array.from(placement.word).map((letter, index) => ({
      letter,
      x: placement.x + (placement.direction === 'across' ? index : 0),
      y: placement.y + (placement.direction === 'down' ? index : 0)
    }))
  );

  const minX = Math.min(...cells.map((cell) => cell.x));
  const minY = Math.min(...cells.map((cell) => cell.y));

  return placements.map((placement) => ({
    ...placement,
    x: placement.x - minX,
    y: placement.y - minY
  }));
}
